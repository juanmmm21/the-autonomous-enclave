/**
 * Debe coincidir con GRID_WIDTH/GRID_HEIGHT en `backend/src/enclave/models.py`,
 * que valida los límites de `Position` sobre las mismas coordenadas.
 */
export const TILE_SIZE = 32;
export const GRID_WIDTH = 80;
export const GRID_HEIGHT = 52;
